using System;

public class program
{
    public static void Main(string[] arg)
    {
        Menu.ExibirMenuPrincipal();
    }
}

{
ConsoleReadLine(===== MENU PRINCIPAL =====)
      1 -ConsoleReadLine(Verificação de idade para entrada em evento)
      2 -ConsoleReadLine(Situação final do estudante)
      3 -ConsoleReadLine(Classificação de temperatura ambiente)
      4 -ConsoleReadLine(Cálculo de compra com desconto)
      5 -ConsoleReadLine(Verificação de meta de produtividade)
      6 -ConsoleReadLine(Classificação por faixa etária)
      7 -ConsoleReadLine(Maior valor entre dois números)
      8 -ConsoleReadLine(Maior valor entre três números)
      9 -ConsoleReadLine(Identificação de número par ou ímpar)
      10 -ConsoleReadLine(Cálculo de passagem por idade)
      11 -ConsoleReadLine(Contagem progressiva)
      12 -ConsoleReadLine(Contagem regressiva)
      13 -ConsoleReadLine(Tabuada de um número)
      14 -ConsoleReadLine(Soma e média de cinco valores)
      15 -ConsoleReadLine(Relatório de números positivos, negativos e zero)
      16 -ConsoleReadLine(Controle de acesso por senha numérica)
      17 -ConsoleReadLine(Validação de nota)
      18 -ConsoleReadLine(Tempo total de atendimento)
      19 -ConsoleReadLine(Simulação de movimentação de caixa)
      20 -ConsoleReadLine(Controle simples de estoque)
ConsoleReadLine(Escolha uma opção:)
}